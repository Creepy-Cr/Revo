I want to integrate Tower Exchange into my existing Revo project on Arc Testnet.

IMPORTANT:

* Do NOT hardcode any API key in source code.
* Read the Tower API key from the environment variable:
  TOWER_API_KEY
* I will add the key myself in Replit Secrets.
* Never print, expose, log, or return the API key anywhere in the UI, browser, logs, API responses, or Git repository.

GOAL:
Replace Revo's currently simulated ETH rebalancing with a REAL on-chain swap using Tower Exchange.

NETWORK:

* Arc Testnet
* Chain ID: 5042002
* Use Arc Testnet RPC where required.

TOWER:

* Base API: https://www.tower.exchange/api/public
* Authentication: Authorization: Bearer ${TOWER_API_KEY}
* Required scope: swaps
* Tower should only be used for obtaining quotes and building unsigned swap transactions.
* Revo must sign transactions using its existing server-side wallet/signer.
* Never send the private key to Tower or expose it to the frontend.

FIRST STEP — VERIFY SUPPORT:
Before changing the existing rebalance logic, implement a safe diagnostic/test flow that:

1. Calls Tower's DEX/token discovery endpoint.
2. Determines whether ETH or WETH and USDC are supported on Arc Testnet.
3. Determines whether there is currently usable liquidity for ETH/WETH → USDC.
4. If ETH/WETH → USDC is NOT supported, do not fake the transaction and do not modify portfolio balances.
5. Clearly report the exact reason why the pair cannot be traded.

QUOTE FLOW:
When ETH/WETH → USDC is supported:

1. Get the current real portfolio balance from Revo's existing wallet/indexer.
2. Calculate the amount of ETH/WETH that must actually be sold according to the active policy.
3. Request a Tower swap quote.
4. Use a configurable slippage limit. Default to 0.5%.
5. Show the quote and proposed transaction to the approval layer.
6. Do NOT execute automatically at the quote stage.

EXECUTION FLOW:
After an authorized operator approves the rebalance:

1. Request/build the unsigned Tower transaction.
2. Validate:

   * chain ID = 5042002
   * expected input token
   * expected output token = USDC
   * input amount
   * minimum acceptable USDC output
   * recipient wallet
   * approved Tower contract/router
3. Sign the transaction using Revo's existing wallet signer.
4. Broadcast the raw transaction to Arc Testnet.
5. Store the transaction hash.
6. Wait for blockchain confirmation.
7. Verify the receipt.
8. Only after successful confirmation, update Revo's database/indexer state.
9. If the transaction fails, do NOT update the portfolio as if the swap happened.

IMPORTANT ACCOUNTING RULE:
The blockchain must be the source of truth.

Do NOT simply change:
ETH: $2,000 → $1,000
USDC: $4,500 → $5,500

Instead:

* Execute the real ETH/WETH → USDC swap.
* Read the resulting on-chain balances.
* Update the database from the confirmed blockchain result.

SAFETY:
Implement these protections:

* Maximum trade amount
* Maximum daily trading amount
* Maximum allowed slippage
* Approved/allowlisted Tower contracts only
* Correct chain ID validation
* Token address validation
* Recipient validation
* Transaction simulation/eth_call where possible before signing
* Emergency trading pause
* Full audit log
* Never execute an AI-generated transaction directly
* AI can create policy/proposal only.
* Deterministic backend code calculates the exact trade.
* Human/operator approval is required before execution unless Revo's explicitly enabled autonomous mode permits it.

REVO POLICY EXAMPLE:

If the portfolio is:

USDC = 45%
ETH = 20%
Lending = 22%
Reserve = 13%

and the active policy says:

"ETH allocation must not exceed 10%."

The deterministic rebalance engine should calculate the required ETH reduction.

Example:
Total portfolio = $10,000
Current ETH = $2,000
Target ETH = $1,000

Required reduction = approximately $1,000 worth of ETH.

Do NOT assume ETH price or hardcode a quantity.
Use the real quote/price and calculate the actual token amount.

IMPORTANT:
If Revo currently represents ETH, aUSDC, or sUSDC as simulated assets, clearly separate:

* REAL on-chain balance
* SIMULATED balance

Do not silently pretend a simulated position became real.

LENDING:
Do not implement fake lending transactions.
Keep existing simulated lending positions clearly marked as SIMULATED until a real Arc-compatible lending protocol is integrated.

UI:
Add a clear transaction status flow:

PROPOSED
→ APPROVED
→ QUOTE RECEIVED
→ TRANSACTION BUILT
→ SIGNING
→ SUBMITTED
→ CONFIRMING
→ CONFIRMED / FAILED

Display:

* Asset being sold
* Asset being received
* Amount
* Expected output
* Minimum output
* Slippage
* Network
* Transaction hash after submission
* Confirmation status

Never display private keys or API keys.

ERROR HANDLING:
Handle:

* Unsupported token
* Unsupported trading pair
* No liquidity
* Quote failure
* Build transaction failure
* Insufficient balance
* Insufficient gas
* Slippage exceeded
* Transaction reverted
* RPC failure
* Timeout
* Confirmation failure

Do not fall back to simulated bookkeeping when a real swap fails.

IMPLEMENTATION APPROACH:
First inspect the existing Revo codebase and identify:

* wallet/custody implementation
* Arc RPC implementation
* balance/indexer implementation
* portfolio calculation
* policy engine
* rebalance engine
* approval flow
* transaction/audit tables

Reuse the existing architecture instead of creating duplicate wallet, database, or policy systems.

Do NOT rewrite unrelated parts of the application.

START WITH:

1. Inspect the existing code.
2. Add Tower API client using TOWER_API_KEY.
3. Verify Tower connectivity.
4. Verify Arc Testnet token/pair support.
5. Test quote only.
6. Show me the result.
7. Only after quote works, implement build/sign/broadcast/confirmation.
8. Only after successful real testnet execution should simulated ETH rebalancing be replaced.

At every stage, keep the implementation compatible with the existing Revo architecture.
