{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "description": "Revo OpenAPI specification and orval codegen configuration",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.27.0"
  }
}
